# OnePassword Connect specific exclusions
# The connect-api and connect-sync containers have liveness probes on ports 8080/8081
# but don't expose these ports in their container spec (they're internal health checks)

liveness-port